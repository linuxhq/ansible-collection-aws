---
- name: Verify
  hosts: all
  environment:
    PATH: '/opt/awscli/bin'

  roles:
    - aws_caller_info
    - ec2_security_group_info
    - ec2_vpc_net_info
    - ec2_vpc_subnet_info

  post_tasks:
    - name: Ensure inventory lists are formatted
      ansible.builtin.set_fact:
        ec2_security_group_list:
          - vpc_id: "{{ _ec2_vpc_net_info_dict['molecule'].id }}"
            security_groups:
              "{{ ec2_security_group_list.0.security_groups |
                  map('combine', {'state': 'absent'}) }}"
        ec2_vpc_igw_list:
          "{{ ec2_vpc_igw_list |
              map('combine', {'state': 'absent'}) }}"
        ec2_vpc_net_list:
          "{{ ec2_vpc_net_list |
              map('combine', {'state': 'absent'}) }}"
        ec2_vpc_subnet_list:
          "{{ ec2_vpc_subnet_list |
              map('combine', {'state': 'absent'}) }}"
        iam_role_list:
          "{{ iam_role_list |
              map('combine', {'state': 'absent'}) }}"
        rds_instance_list:
          "{{ rds_instance_list |
              map('combine', {'state': 'absent'}) }}"
        rds_instance_param_group_list:
          "{{ rds_instance_param_group_list |
              map('combine', {'state': 'absent'}) }}"
        rds_option_group_list:
          "{{ rds_option_group_list |
              map('combine', {'state': 'absent'}) }}"
        rds_subnet_group_list:
          "{{ rds_subnet_group_list |
              map('combine', {'state': 'absent'}) }}"

    - name: Ensure aws rds instances are deleted
      ansible.builtin.include_role:
        name: rds_instance

    - name: Ensure aws rds option groups are released
      amazon.aws.rds_option_group:
        option_group_name: "{{ _og.option_group_name }}"
        state: absent
        validate_certs: true
      register: __rds_option_group_release
      loop: "{{ rds_option_group_list }}"
      loop_control:
        label: "{{ _og.option_group_name | d(none) }}"
        loop_var: _og
      delay: 30
      retries: 20
      until:
        - __rds_option_group_release is success

    - name: Ensure roles are included
      ansible.builtin.include_role:
        name: "{{ _verify }}"
      loop:
        - rds_subnet_group
        - rds_option_group
        - rds_instance_param_group
        - ec2_security_group
        - ec2_vpc_subnet
        - ec2_vpc_igw
        - ec2_vpc_net
        - iam_role
      loop_control:
        label: "{{ _verify }}"
        loop_var: _verify
...
