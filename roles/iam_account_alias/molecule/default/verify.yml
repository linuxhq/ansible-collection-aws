---
- name: Verify
  hosts: all
  pre_tasks:
    - name: Ensure inventory lists are formatted
      ansible.builtin.set_fact:
        iam_account_alias_state: absent

  roles:
    - iam_account_alias
...
