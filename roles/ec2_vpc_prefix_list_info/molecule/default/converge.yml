---
- name: Converge
  hosts: all
  environment:
    PATH: "/opt/awscli/bin:{{ ansible_env.PATH }}"

  roles:
    - role: ec2_vpc_prefix_list
      tags:
        - molecule-idempotence-notest

    - ec2_vpc_prefix_list_info
...
