---
- name: Converge
  hosts: all
  environment:
    PATH: '/opt/awscli/bin'

  roles:
    - role: sqs_queue
      sqs_queue_list: []
      tags:
        - molecule-idempotence-notest

    - aws_az_info
    - aws_caller_info

    - role: sqs_queue
      tags:
        - molecule-idempotence-notest

    - role: sqs_queue
      sqs_queue_list: "{{ sqs_queue_idempotence }}"
...
