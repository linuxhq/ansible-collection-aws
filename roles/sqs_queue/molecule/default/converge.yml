---
- name: Converge
  hosts: all
  environment:
    PATH: '/opt/awscli/bin'

  roles:
    - aws_az_info
    - aws_caller_info

    - role: sqs_queue
      sqs_queue_list: []
      tags:
        - molecule-idempotence-notest

    - sqs_queue
...
