---
- name: Ensure aws route53 zone associations are batched
  tags:
    - route53_zone_associate
  ansible.builtin.include_tasks:
    apply:
      tags:
        - route53_zone_associate
    file: include.yml
  loop:
    "{{ route53_zone_associate_list |
        batch(route53_zone_associate_batch) }}"
  loop_control:
    label: "{{ __route53_zone_associate_list | length }}"
    loop_var: __route53_zone_associate_list
...
