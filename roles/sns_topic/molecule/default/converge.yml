---
- name: Converge
  hosts: all
  environment:
    PATH: '/opt/awscli/bin'

  roles:
    - aws_az_info
    - aws_caller_info

    - role: sqs_queue
      tags:
        - molecule-idempotence-notest
      check_mode: false

    - role: sns_topic
      sns_topic_list: []
      tags:
        - molecule-idempotence-notest

    - role: sns_topic
      tags:
        - molecule-idempotence-notest

    - role: sns_topic
      sns_topic_list: "{{ sns_topic_updated_list }}"
...
