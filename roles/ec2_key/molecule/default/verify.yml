---
- name: Verify
  hosts: all
  pre_tasks:
    - name: Ensure inventory lists are formatted
      ansible.builtin.set_fact:
        ec2_key_list:
          "{{ ec2_key_list |
              map('combine', {'state': 'absent'}) }}"

  roles:
    - ec2_key
...
