---
- name: Converge
  hosts: all
  environment:
    PATH: '/opt/awscli/bin'

  roles:
    - role: secretsmanager_secret
      secretsmanager_secret_list: []
      tags:
        - molecule-idempotence-notest

    - role: secretsmanager_secret
      tags:
        - molecule-idempotence-notest

    - role: secretsmanager_secret
      secretsmanager_secret_list: "{{ secretsmanager_secret_updated_list }}"
...
