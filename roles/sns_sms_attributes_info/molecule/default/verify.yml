---
- name: Verify
  hosts: all
  roles:
    - sns_sms_attributes_info

  tasks:
    - name: Ensure info is verified
      ansible.builtin.assert:
        quiet: true
        that:
          - _sns_sms_attributes_info_dict is mapping
          - _sns_sms_attributes_info_dict.monthly_spend_limit is defined
...
