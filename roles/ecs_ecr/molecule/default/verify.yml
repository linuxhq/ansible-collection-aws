---
- name: Verify
  hosts: all
  pre_tasks:
    - name: Ensure inventory lists are formatted
      ansible.builtin.set_fact:
        ecs_ecr_list:
          "{{ ecs_ecr_list |
              map('combine', {'state': 'absent'}) }}"

  roles:
    - ecs_ecr
...
