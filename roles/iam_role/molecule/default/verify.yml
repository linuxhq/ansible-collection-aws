---
- name: Verify
  hosts: all
  pre_tasks:
    - name: Ensure inventory lists are formatted
      ansible.builtin.set_fact:
        iam_role_list:
          "{{ iam_role_list |
              map('combine', {'state': 'absent'}) }}"

  roles:
    - iam_role
...
