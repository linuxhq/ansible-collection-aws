---
- name: Converge
  hosts: all
  environment:
    PATH: "/opt/awscli/bin:{{ ansible_env.PATH }}"

  roles:
    - role: ec2_key
      tags:
        - molecule-idempotence-notest
      check_mode: false

    - ec2_key_info
...
