---
bugfixes:
  - ec2_serial_console - fail without changing access when EC2 returns a
    malformed serial console status.
  - ec2_serial_console_info - fail clearly when EC2 returns a malformed serial
    console status.
