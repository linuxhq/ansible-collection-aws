---
minor_changes:
  - ses_credential - accept C(aws_region) as an alias for the required C(region)
    option.
  - ses_credential - report a missing C(aws_secret_access_key) through Ansible's
    required-option handling instead of the lookup's validation error.
